<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidApp" parent="Base.Theme.AndroidApp" />

    <style name="CalendarTextAppearance.Light" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/black</item>
        <!-- Other text appearance properties -->
    </style>

    <style name="CalendarTextAppearance.Dark" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/darker_gray</item>
        <!-- Other text appearance properties -->
    </style>

</resources>