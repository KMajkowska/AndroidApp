<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidApp" parent="Base.Theme.AndroidApp" />

    <!-- styles.xml -->

    <style name="CalendarTextAppearance_Dark" parent="TextAppearance.AppCompat">
        <!-- You can also define other text appearance attributes as needed -->
        <item name="colorAccent">#5C33F8</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="CalendarTextAppearance_Light" parent="TextAppearance.AppCompat">
        <item name="colorAccent">#3894FF</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>


</resources>